apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "kafka-apps.fullname" . }}-consumer
  labels:
    app: consumer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: consumer
  endpoints:
  - port: metrics
    interval: 15s
