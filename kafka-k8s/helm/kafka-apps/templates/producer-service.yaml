apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-apps.fullname" . }}-producer
  labels:
    app: producer
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.producer.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: producer
