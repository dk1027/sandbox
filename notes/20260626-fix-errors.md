- 2026-06-26: Fixed the Kafka producer/consumer crashloops caused by an invalid OpenTelemetry import path (`BatchSpanProcessor` was imported from `opentelemetry.sdk.trace` instead of `opentelemetry.sdk.trace.export`, and status handling now uses `Status`/`StatusCode` from `opentelemetry.trace`). Validation: rebuilt and pushed both images, restarted the apps deployments, and both pods are now Running with 0 restarts.
- 2026-06-26: Fixed the Grafana init crashloop by removing the external dashboard download URLs from the kube-prometheus-stack values; the cluster already loads dashboards from the local `grafana_dashboard: "1"` ConfigMaps. Validation: upgraded the monitoring release and confirmed the Grafana pod is Running.
