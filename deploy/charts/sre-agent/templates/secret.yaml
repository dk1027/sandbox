apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sre-agent.fullname" . }}-secret
  labels:
    {{- include "sre-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  LLM_API_KEY: {{ .Values.llm.apiKey | quote }}
