apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sre-agent.fullname" . }}-llm-server
  labels:
    {{- include "sre-agent.labels" . | nindent 4 }}
data:
  openai_server.py: |
{{ .Files.Get "files/openai_server.py" | nindent 4 }}
